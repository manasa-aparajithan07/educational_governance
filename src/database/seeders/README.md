# Database Seeders

This directory is designated for database seed definitions.

Default development seed logic is implemented in `src/database/seed.js`, which securely provisions initial departments, courses, and accounts with Bcrypt hashed passwords.

To run database seeding:
```bash
npm run db:seed
```
